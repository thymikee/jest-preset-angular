{
  "name": "example-app-yarn-workspace",
  "version": "17.0.0",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "private": true,
  "scripts": {
    "test": "yarn workspaces foreach -A run test",
    "test-isolated": "yarn workspaces foreach -A run test-isolated",
    "test-esm": "yarn workspaces foreach -A run test-esm",
    "test-esm-isolated": "yarn workspaces foreach -A run test-esm-isolated"
  }
}
